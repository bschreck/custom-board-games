Generate the wording for the instructions of this new board game by filling out the rest of the provided YAML template,
You already know the template. You should output a YAML file that would populate the webpage for the game {new_game_name}.
This file will be used as config to populate the copy of a website, so you need to fill out every field and make sure it is parseable by YAML.
You must include quotes around strings because you might use reserved keywords by YAML.

This board game uses the exact same rules as the existing game {existing_game_name}, but all the characters and terminology are generated by you and match the theme & story you already generated.
