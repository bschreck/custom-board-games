Generate the wording for the instructions of this new board game by filling out the rest of the provided JSON template,
The example for the original game will be provided first. You should output a JSON file that would allow a program to autopopulate the webpage for the game {new_game_name}.
Therefore, you need to fill out every field and make sure the exact output string generated is parseable by JSON.

This board game uses the exact same rules as the existing game {original_game_name}, but all the characters and terminology are generated by you and match the theme & story you already generated.

---- TEMPLATE ----
{template_str}
